import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
          main:'#001430',
        },
        secondary: {
          main: '#0cb8b6',
        }
      },
  });

  export default theme;